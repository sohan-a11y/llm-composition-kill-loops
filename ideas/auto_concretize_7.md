# Auto-concretize batch 7 (FINAL): 10 stubs → precise claims + §1e screen, 2026-09-23
# Concretization queue now COMPLETE: 65/65 stubs processed (5+10+10+10+10+10+10).
# §1e only; web + Filter-2 legs pending for all concretized claims.

### L1 (tooling + bit-exact) → "Bit-exact eval pins: hash (weights, dataset version) into every reported score"
§1e-adjacent: D10 contamination/provenance (PARTIAL). PARTIAL-leaning — must cite D10/provenance work; delta is binding triple as score suffix. Narrow.

### L2 (econ + kill control) → "Cheapest-first spot preemption for batch queues"
§1e: no hit. BORDERLINE (scheduling policy, thin; web leg must check cluster-scheduling literature). Low traction pre-registered.

### L3 (os + kill control) → "OOM-score request kill with refund under memory pressure"
§1e-adjacent: E9 OOM-killer (§1e-FAIL, KV eviction family). Likely FAIL — request-level vs prefix-level is a thin delta on a failed idea. Marked BORDERLINE pending proof it escapes E9's exclusion.

### L4 (signal + kill control) → "Logit-outlier clipping counter as anomaly signal (clip beyond Nσ, count clips)"
§1e: no direct hit (LLM.int8 outlier work is quantization-motivated, distinct purpose). BORDERLINE — Filter-2 must beat entropy/perplexity baselines (probably collapses to confidence; skepticism pre-registered).

### L5 (bio + kill control) → "Hebbian fast-weights session memory (outer-product updates, decayed)"
§1e-adjacent: fast weight programmers (excluded: mainstream since Schlag/Irie/Schmidhuber 2021). Likely FAIL — family-owned. Marked BORDERLINE pending proof of delta vs FWPs.

### L6 (invert + bit-exact) → "Greedy-only cross-hardware reproducibility CI gate"
§1e-adjacent: E33 determinism badge (PARTIAL/wrapper). PARTIAL-leaning — must cite E33 + LLM-42/CoRun; delta is greedy-only scope (narrower, cheaper, fewer flapping sources).

### L7 (compilers + bit-exact) → "Typechecked prompt templates rejected pre-deploy when ill-typed"
§1e-adjacent: typed decision endpoints ("TypeSafe AI" clones, excluded) cover typed OUTPUTS; template input-types are a distinct level. BORDERLINE — advances with note; web leg must confirm the gap (template type systems vs endpoint typing).

### L8 (ecc + bit-exact) → "Per-shard weight checksums verified at multi-GPU serve load"
§1e-adjacent: E16 single-signer signing (PARTIAL). PARTIAL-leaning — must cite sigstore/E16; delta is shard-level + serve-time verification. Narrow.

### L9 (crypto + bit-exact) → "Signed per-request inference receipts (input-hash, output-hash, model-version)"
§1e-adjacent: D4 provenance triple (PARTIAL) + C2PA. PARTIAL-leaning — must cite both; delta is receipt protocol (verify-after-serve) vs span stamping.

### L10 (db + bit-exact) → "Snapshot-pinned eval corpora per run (versioned snapshot ID in results)"
§1e-adjacent: eval harnesses (excluded) + D10 contamination. BORDERLINE — advances as reproducibility practice with pre-registered skepticism (snapshot ≠ leakage control).

## Tally: 10 concretized (0 PASS-leaning, 4 PARTIAL-leaning L1/L6/L8/L9, 6 BORDERLINE L2/L3/L4/L5/L7/L10 incl. 2 likely-dead L3/L5).
## Queue COMPLETE. Next: web legs for concretized claims (65) + T4 kill-runs + final ranked table.
